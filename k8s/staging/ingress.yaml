apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-monnom-MonApplication-frontend-ingress
  namespace: staging-monnom-MonApplication-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.monsousdomain2.flapi.org
      secretName: staging-monnom-MonApplication-frontend-tls
  rules:
    - host: staging.monsousdomain2.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-monnom-MonApplication-frontend-service
                port:
                  number: 3000
