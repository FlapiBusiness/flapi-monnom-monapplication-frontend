apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-monnom-MonApplication-frontend-ingress
  namespace: prod-monnom-MonApplication-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - monsousdomain2.flapi.org
      secretName: prod-monnom-MonApplication-frontend-tls
  rules:
    - host: monsousdomain2.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: prod-monnom-MonApplication-frontend-service
                port:
                  number: 3000
