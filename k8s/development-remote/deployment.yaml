apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-monnom-MonApplication-frontend-deployment
  namespace: dev-monnom-MonApplication-frontend
  labels:
    app: dev-monnom-MonApplication-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev-monnom-MonApplication-frontend
  template:
    metadata:
      labels:
        app: dev-monnom-MonApplication-frontend
    spec:
      containers:
        - name: dev-monnom-MonApplication-frontend
          image: corentin35/monnom-MonApplication-frontend:develop-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: '400m'
              memory: '512Mi'
            limits:
              cpu: '800m'
              memory: '1Gi'
      imagePullSecrets:
        - name: regcred
