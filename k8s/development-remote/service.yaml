apiVersion: v1
kind: Service
metadata:
  name: dev-monnom-MonApplication-frontend-service
  namespace: dev-monnom-MonApplication-frontend
spec:
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: dev-monnom-MonApplication-frontend
