apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-monnom-MonApplication-frontend-ingress
  namespace: dev-monnom-MonApplication-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - dev.monsousdomain2.flapi.org
      secretName: dev-monnom-MonApplication-frontend-tls
  rules:
    - host: dev.monsousdomain2.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: dev-monnom-MonApplication-frontend-service
                port:
                  number: 3000
